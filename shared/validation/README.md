# Shared Validation

Future home for application-neutral validation result types and topology checks.

A useful shared vocabulary should distinguish `FAILED`, `AMBIGUOUS`, `OBSERVED`, `VERIFIED`, and `SAFE_CHECKPOINT` rather than representing all outcomes with one boolean.
