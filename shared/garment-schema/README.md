# Shared Garment Schema

This directory contains application-neutral contracts shared by DXF generation, CLO automation, Blender dressing, and future tools.

Semantic IDs are stable project data. Application-local indices are runtime data.

Current draft: `garment.schema.json` v0.1.0.
