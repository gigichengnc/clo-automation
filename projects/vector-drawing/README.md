# Semantic Vector Drawing — v0.1

A deterministic vector-drawing prototype for clean fashion flats.

The AI-facing layer should describe **what exists**. This package owns **how the lines are drawn**.

```text
prompt / reference interpretation
        ↓
structured garment spec
        ↓
semantic drawing model
        ↓
deterministic geometry
        ↓
SVG (authoritative illustration)
DXF (CAD-friendly artwork export)
```

## Why

Raster image generation is good at visual ideation but unreliable for technical line quality. It can create doubled contours, waviness, accidental asymmetry and inconsistent construction details. This prototype replaces pixel guessing with named curves and explicit symmetry.

## v0.1 scope

Supported structured input:

- garment: `dress`
- neckline: `round | v`
- sleeve: `sleeveless | short`
- silhouette: `a_line | straight`
- length: `mini | knee | midi | maxi`

The output contains stable semantic path IDs such as:

```text
outline.neckline
outline.shoulder.left
outline.armhole.left
outline.side.left
outline.hem
guide.centre_front
```

Left/right geometry is generated by exact mirroring where symmetry is intended.

## Run

From this directory:

```bash
python -m vector_drawing.cli examples/round_neck_a_line.json \
  --svg out/dress.svg \
  --dxf out/dress.dxf
```

Run tests:

```bash
python -m unittest discover -s tests -v
```

## Important DXF boundary

The v0.1 DXF exporter writes **standard DXF R12 artwork geometry** by deterministically sampling cubic curves into polylines.

It is **not** DXF-AAMA / DXF-ASTM and must not be confused with the repository's factory garment-pattern pipeline.

SVG is the authoritative illustration output in v0.1.

## Next milestones

1. semantic prompt parser that outputs only validated enums/parameters;
2. front + back flats and more construction details;
3. curve continuity / intersection validators;
4. semantic raster-to-vector cleanup from AI reference images;
5. shared geometry kernel with garment-pattern code where semantics genuinely overlap;
6. optional production-pattern mapping only after illustration and manufacturing semantics are explicitly separated.
